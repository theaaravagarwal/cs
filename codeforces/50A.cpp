#include <bits/stdc++.h>
typedef long long ll;
using namespace std;
void solve() {
    short a, b;
    cin>>a>>b;
    cout<<(a*b)/2<<'\n';
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    solve();
    return 0;
}